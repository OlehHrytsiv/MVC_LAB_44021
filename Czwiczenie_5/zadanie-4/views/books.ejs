<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books</title>
</head>
<body>
    <h1>Books</h1>
    <ul>
        <% if (books.length > 0) { %>
            <% books.forEach(book => { %>
                <li>
                    <%= book.title %>
                    <form action="/book/delete/<%= book.id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit">Delete</button>
                    </form>
                    <a href="/book/<%= book.id %>">View Details</a>
                </li>
            <% }) %>
        <% } else { %>
            <li>No books have been found</li>
        <% } %>
    </ul>
    <%- include('partials/navigation') %>
</body>
</html>